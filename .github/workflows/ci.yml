name: CI — Build & Type Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: power-system-ui/package-lock.json

      - name: Install dependencies
        working-directory: power-system-ui
        run: npm ci

      - name: TypeScript type check
        working-directory: power-system-ui
        run: npx tsc --noEmit

      - name: Production build
        working-directory: power-system-ui
        run: npm run build

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: power-system-ui/dist/
          retention-days: 7

  backend-tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: power-system-api/requirements-dev.txt

      - name: Install test dependencies
        working-directory: power-system-api
        run: pip install -r requirements-dev.txt

      - name: Run calculator unit tests
        working-directory: power-system-api
        run: python -m pytest tests/ -v
